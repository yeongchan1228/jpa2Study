#spring:
#  datasource:
#    url: jdbc:h2:mem:test  # h2를 메모리 모드로 실행해서 테스트하기 좋게 만든다.
#    driver-class-name: org.h2.Driver
#    username: sa
#    password:

#  jpa:
 #   hibernate:
  #    ddl-auto: create
   # properties:
    #  hibernate:
     #    show_sql: true // sql? sout? ??
      #  format_sql: true
# 스프링 부트의 테스트에서는 이런 설정이 없으면 알아서 jdbc:h2:mem:testdb로 연결해서 메모리 상에서 실행한다.


logging:
  level:
    org.hibernate.SQL: debug # sql? log? ??
    org.hibernate.type: trace # ?? ????? ???? ??